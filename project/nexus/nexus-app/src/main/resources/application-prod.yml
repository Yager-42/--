# 生产环境占位配置
logging:
  level:
    root: WARN

feed:
  inbox:
    ttlDays: 14
  outbox:
    ttlDays: 14

reaction:
  sync:
    mode: redis
  count:
    model: snapshot

spring:
  rabbitmq:
    publisher-confirm-type: correlated
    publisher-returns: true
    template:
      mandatory: true
