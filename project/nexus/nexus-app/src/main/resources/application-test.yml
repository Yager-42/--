# 测试环境占位配置
logging:
  level:
    cn.nexus: INFO

feed:
  inbox:
    ttlDays: 14
  timeline:
    maxSize: 1000
    ttlDays: 30
  outbox:
    ttlDays: 14

reaction:
  sync:
    mode: redis
  count:
    model: snapshot

spring:
  rabbitmq:
    publisher-confirm-type: correlated
    publisher-returns: true
    template:
      mandatory: true
