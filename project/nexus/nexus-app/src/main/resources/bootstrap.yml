spring:
  config:
    activate:
      on-profile: "!wsl"
  application:
    name: nexus
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}
        file-extension: yml
        group: DEFAULT_GROUP
        refresh-enabled: true
