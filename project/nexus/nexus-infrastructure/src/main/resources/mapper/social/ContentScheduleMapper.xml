<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.nexus.infrastructure.dao.social.IContentScheduleDao">

    <resultMap id="ContentScheduleMap" type="cn.nexus.infrastructure.dao.social.po.ContentSchedulePO">
        <id property="taskId" column="task_id"/>
        <result property="userId" column="user_id"/>
        <result property="contentData" column="content_data"/>
        <result property="scheduleTime" column="schedule_time"/>
        <result property="status" column="status"/>
        <result property="retryCount" column="retry_count"/>
    </resultMap>

    <insert id="insert" parameterType="cn.nexus.infrastructure.dao.social.po.ContentSchedulePO">
        INSERT INTO content_schedule(task_id, user_id, content_data, schedule_time, status, retry_count)
        VALUES(#{taskId}, #{userId}, #{contentData}, #{scheduleTime}, #{status}, #{retryCount})
    </insert>

    <update id="updateStatus">
        UPDATE content_schedule
        SET status = #{status},
            retry_count = #{retryCount}
        WHERE task_id = #{taskId}
    </update>

    <select id="selectPending" resultMap="ContentScheduleMap">
        SELECT task_id, user_id, content_data, schedule_time, status, retry_count
        FROM content_schedule
        WHERE status = 0
          AND schedule_time <= #{before}
        <if test="limit != null">
            LIMIT #{limit}
        </if>
    </select>

</mapper>
