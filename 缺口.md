值得做：因为它们都是用户可见的“功能失效点”，不补就是假功能。

【缺口清单（文档要求但项目未实现/未打通）】

建表/迁移缺口：文档要求执行 interaction_comment + interaction_comment_pin DDL（comment-floor-system-implementation.md (line 644)），但仓库内没看到这两张表的 DDL/迁移脚本；现有 schema 示例也不包含它们（social_schema.sql (line 1)）。
@提及通知未实现：文档要求在写评论时对 mentions 逐人发布 InteractionNotifyEvent(eventType=COMMENT_MENTIONED)（comment-floor-system-implementation.md (line 151)、comment-floor-system-implementation.md (line 449)），但当前写评论只把 mentions 当占位，不发任何通知事件（InteractionService.java (line 87)）；项目内也找不到 InteractionNotifyEvent/COMMENT_MENTIONED 的实现痕迹。
点赞→热榜/回写未打通：文档要求 applyReaction 在 targetType=COMMENT 且 delta!=0 时发布 CommentLikeChangedEvent（comment-floor-system-implementation.md (line 574)、comment-floor-system-implementation.md (line 589)）。项目虽有消费者（CommentLikeChangedConsumer.java (line 28)）和端口定义（ICommentEventPort.java (line 18)），但点赞链路并不会发布该事件（ReactionLikeService.java (line 44)；InteractionService.java (line 38) 只调用 like service）。结果：interaction_comment.like_count 不会被更新，热榜也不会因点赞刷新。
热榜冷启动/重建缺失：文档把“热榜重建”标为必补（comment-floor-system-implementation.md (line 354)、comment-floor-system-implementation.md (line 370)），但项目里没有脚本/定时任务/服务去扫描 MySQL 并重建 comment:hot:{postId}；Redis 被清空或上线前有存量评论时，热榜会空。
如果你要我下一步直接推动落地，最短闭环顺序是：

补 MySQL DDL/迁移（两张表）
评论写入补发 InteractionNotifyEvent（@提及）
点赞链路补发 CommentLikeChangedEvent（delta!=0 才发）
加一个“按 postId 重建热榜”的脚本或定时任务（不走读路径 fallback）


分发

值得做：缺口非常明确，做完你就知道下一步要补什么、哪些文档只是未来计划。

你这份文档里，当前代码确实“还没实现/没落地”的部分主要是这 4 类：

Phase 3 推荐与排序：文档明确标为未完成（distribution-feed-implementation.md (line 81)），并且 Phase 3 细节（比如 feed:global:latest 兜底）只存在于文档描述（distribution-feed-implementation.md (line 1771)），代码侧没有对应实现。
HotKey + L1 本地缓存（JD HotKey + Caffeine）：文档明确写“当前不做”（distribution-feed-implementation.md (line 94)）。仓库里虽然已经有 HotKey/Caffeine 的用法示例（点赞缓存里用到了：ReactionCachePort.java (line 8)），但 Feed 的回表入口（文档要求放在 ContentRepository）并没有按文档把“热点判定→短 TTL L1 缓存”接起来。
“铁粉集合生成”本身：文档在粉丝分层里明确写“铁粉集合生成也未实现”（distribution-feed-implementation.md (line 85)）。代码也明确只做了“读取契约”，生成策略不在本阶段（IFeedCoreFansRepository.java (line 8)），且仓库里没有任何写入/生成 feed:corefans:{authorId} 的逻辑。
unfollow 相关（接口/事件/补偿）：文档写 unfollow 仍是“文档预留”（distribution-feed-implementation.md (line 1147)）；当前关系接口只有 /follow，没有 /unfollow（RelationController.java (line 31)），所以文档里对 unfollow 的方案目前都还没落地。