# 缺口.md

日期：2026-01-26  
执行者：Codex（Linus-mode）

这份文件只记录“文档要求但代码还没落地”的真实缺口。做完就从这里删掉，别让它变成垃圾 TODO。

## 已补齐（从缺口清单移除）

- 评论链路：`interaction_comment` / `interaction_comment_pin` DDL 已追加到 `project/nexus/docs/social_schema.sql`
- @提及通知：评论创建后发布 `InteractionNotifyEvent(eventType=COMMENT_MENTIONED)`，并由通知消费者聚合入库
- 点赞→评论热榜回写：点赞链路在 `targetType=COMMENT && delta!=0` 发布 `CommentLikeChangedEvent`，消费者回写 `like_count` 并刷新热榜
- 热榜冷启动/重建：已提供按 postId 的重建服务/runner

- 分发/Feed：
  - unfollow 全链路已落地（API + 事件 + Feed 侧生效）
  - 铁粉集合 `feed:corefans:{authorId}` 的写入/TTL 已落地（消费 `interaction.notify` 旁路）
  - HotKey + L1 本地缓存已落地（ContentRepository 对热点 postId 启用 Caffeine 短 TTL）

## 仍未完成

- （无）
